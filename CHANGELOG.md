# 📜 NEXA Ultimate - Changelog
## الإصدار v3.1: تحديث النواة الموحدة (Unified Core Update)
**التاريخ:** 05-02-2026

يعتبر هذا التحديث هو الأضخم في تاريخ النظام، حيث تم الانتقال من معمارية الخدمات المصغرة المنفصلة (Microservices) إلى معمارية النواة الموحدة (Unified Monolith)، مع إعادة بناء شاملة لنظام التخزين.

---

### 1. 🏗️ البنية التحتية والنواة (Core Architecture)
*   **⚠️ توحيد الخدمات:** دمج 7 خدمات (Server, Gateway, Admin, Chat, Storage, Dashboard, DNS) في ملف تنفيذي واحد (`nexa.exe`).
*   **🚀 التشغيل المتوازي:** استخدام `Go Routines` و `sync.WaitGroup` لتشغيل كافة الأنظمة في وقت واحد باستهلاك ذاكرة أقل وسرعة تشغيل أعلى.
*   **⚙️ ملف تكوين مركزي:** إنشاء `pkg/config/config.go` لتوحيد عناوين المنافذ (Ports) ومنع التضارب بين الخدمات.

### 2. 💾 نظام التخزين السحابي (Cloud Storage 2.0)
تمت إعادة كتابة خدمة `storage` بالكامل لتتحول من مجرد "مدير ملفات" إلى "سحابة شخصية متكاملة":
*   **💎 واجهة Glassmorphism:** تصميم عصري وجذاب مع دعم الوضع الليلي (Dark Mode) وتأثيرات الزجاج.
*   **📱 القائمة الجانبية (Sidebar):** تنقل سريع بين (الوارد، المشترك، الخزنة، النسخ الاحتياطي) مع فلاتر للملفات (صور، فيديو، مستندات).
*   **🔐 الخزنة (Vault):** إضافة مجلد سري محمي برمز مرور (`PIN`) لتشفير الوصول للملفات الحساسة.
*   **🔄 النسخ الاحتياطي الآلي (Auto-Backup):** عملية خلفية (Background Job) تقوم بنسخ الملفات الجديدة تلقائياً لمجلد آمن كل 5 دقائق.
*   **🔗 المشاركة الذكية:**
    *   توليد **QR Code** فوري لأي ملف لمشاركته مع الهواتف دون تطبيقات.
    *   إنشاء روابط مباشرة قصيرة (`/s/xyz`).
    *   زر **فتح في نافذة جديدة** للمشاهدة المباشرة.
*   **🔍 بحث وتصفية:** شريط بحث فوري داخل الملفات، وفرز حسب النوع.
*   **📥 السحب والإفلات:** دعم `Drag & Drop` لرفع الملفات بسهولة.

### 3. 🌐 الشبكة والاتصال (Network & Connectivity)
*   **🔌 إصلاح بروكسي الداشبورد:** تم تعديل كود الـ JavaScript في التخزين لاستخدام **مسارات نسبية (Relative Paths)** بدلاً من المطلقة، مما أصلح مشكلة "Connection Error" عند استخدامه عبر الداشبورد.
*   **📊 تحديث الداشبورد:** الآن تعرض الداشبورد الحالة الحقيقية للأجهزة وتربط المستخدم بالمنافذ الصحيحة الجديدة.
*   **🛡️ أمان الرفع:** تحسين التعامل مع الملفات المكررة (Renaming) ومنع مسارات الاختراق (Path Traversal).

### 4. 🛠️ التحسينات التقنية (Technical Improvements)
*   **📦 Embedding:** تضمين ملفات HTML/CSS/JS داخل ملف `Go` الثنائي، مما يسهل النشر (نحتاج فقط لنقل ملف واحد `nexa.exe`).
*   **🧹 تنظيف الكود:** إزالة الملفات الزائدة ونسخ `index.html` القديمة المكررة.
*   **📜 التوثيق:** تحديث ملف `README.md` بالكامل ليعكس الهيكلة الجديدة.

---

### 🟢 الخلاصة
تحول نظام **NEXA** من مجرد "سكريبتات متفرقة" إلى **نظام تشغيل محلي (Local Cloud OS)** قوي، مستقر، وبواجهة مستخدم احترافية تنافس الأنظمة التجارية.
