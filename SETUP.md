# Nexa Protocol - نظام شبكة عام مفتوح

نظام شبكة احترافي متكامل مع بروتوكول مخصص وDNS وتشفير كامل. متاح للاستخدام من أي جهاز على الشبكة.

## 🚀 المميزات الرئيسية

✅ **بروتوكول شبكة مخصص (Nexa)**
- أوامر: PING, FETCH, PUBLISH, LIST
- يعمل على TCP/IP

✅ **نظام DNS مخصص (.nexa)**
- RESOLVE, REGISTER, UPDATE, DELETE, LIST
- مثل DNS الحقيقي لكن مخصص

✅ **تشفير TLS كامل**
- جميع الاتصالات مشفرة وآمنة
- شهادات ومفاتيح في مجلد certs/

✅ **لوحة تحكم احترافية**
- إدارة مستخدمين
- سجل العمليات
- إرسال الأوامر من الويب

✅ **نظام مستخدمين متقدم**
- أدوار مختلفة: admin, user, guest
- كلمات مرور مشفرة

✅ **متاح للشبكة**
- متاح من أي جهاز على الشبكة
- 0.0.0.0:1413 للخادم الرئيسي
- 0.0.0.0:1112 لـ DNS
- 0.0.0.0:8080 للوحة التحكم

---

## 📦 المكونات

```
nexa/
├── server/              # الخادم الرئيسي (port 1413 - TLS)
├── dns/                 # خادم DNS (port 1112 - TLS)
├── client/              # عميل سطر أوامر ذكي
├── dns_client.go        # أداة إدارة DNS
├── gui/                 # واجهات الويب
│   ├── admin.go         # لوحة التحكم الاحترافية
│   └── web.go           # واجهة ويب بسيطة
├── certs/               # شهادات TLS
│   ├── cert.pem         # شهادة SSL
│   └── key.pem          # مفتاح خاص
├── users.json           # قاعدة بيانات المستخدمين
└── readme.md            # هذا الملف
```

---

## 🔧 التثبيت والتشغيل

### المتطلبات
- Go 1.25.5 أو أحدث
- أي متصفح إنترنت

### الخطوة 1: شغّل خادم DNS
```bash
cd dns
go run dns_server.go
```
يجب أن ترى:
```
DNS Server starting with TLS on 0.0.0.0:1112
--- DNS Server ready (TLS) ---
```

### الخطوة 2: شغّل خادم Nexa
```bash
cd server
go run server.go
```
يجب أن ترى:
```
Server running with TLS on port: 0.0.0.0:1413
Server is ready with TLS, Waiting for connections
```

### الخطوة 3: شغّل لوحة التحكم
```bash
cd gui
go mod tidy  # تحميل المكتبات المطلوبة
go run admin.go
```
يجب أن ترى:
```
Nexa Admin Panel running at http://0.0.0.0:8080 ...
```

---

## 🌐 الوصول إلى النظام

### من نفس الجهاز:
- **لوحة التحكم:** http://localhost:8080
- **الخادم الرئيسي:** localhost:1413
- **DNS:** localhost:1112

### من جهاز آخر على الشبكة:
- **لوحة التحكم:** http://192.168.1.X:8080 (استبدل X بآخر رقم IP لجهازك)
- **الخادم الرئيسي:** 192.168.1.X:1413
- **DNS:** 192.168.1.X:1112

للحصول على IP جهازك:
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
```

---

## 👥 بيانات الدخول الافتراضية

| المستخدم | كلمة المرور | الدور |
|---------|-----------|------|
| admin | admin123 | مسؤول |
| user1 | admin123 | مستخدم |
| user2 | admin123 | مستخدم |
| guest | admin123 | زائر |

---

## 💻 استخدام العميل من سطر الأوامر

### أوامر بسيطة (بدون DNS):
```bash
# اختبار الاتصال
go run client/client.go PING

# تخزين بيانات
go run client/client.go PUBLISH homepage "مرحباً بك"

# جلب البيانات
go run client/client.go FETCH homepage

# عرض جميع البيانات
go run client/client.go LIST
```

### أوامر DNS:
```bash
# جلب بيانات عبر DNS
go run client/client.go FETCH test.nexa

# تخزين بيانات باسم DNS
go run client/client.go PUBLISH test.nexa "Hello DNS"
```

### إدارة DNS:
```bash
# عرض جميع السجلات
go run dns_client.go LIST

# البحث عن سجل
go run dns_client.go RESOLVE test.nexa

# تسجيل سجل جديد
go run dns_client.go REGISTER mysite.nexa 127.0.0.1 1413 web

# تحديث سجل موجود
go run dns_client.go UPDATE mysite.nexa 127.0.0.1 1413 web

# حذف سجل
go run dns_client.go DELETE mysite.nexa
```

---

## 🛠️ لوحة التحكم (Dashboard)

### المميزات:

1. **ملخص النظام**
   - عدد المستخدمين
   - عدد السجلات
   - آخر نشاط

2. **إدارة المستخدمين** (للمسؤولين فقط)
   - إضافة مستخدمين جدد
   - اختيار الدور (admin, user, guest)
   - حذف المستخدمين

3. **إرسال الأوامر**
   - واجهة سهلة لإرسال أوامر Nexa
   - دعم DNS
   - عرض النتائج فوراً

4. **سجل العمليات**
   - تسجيل كل أمر مع المستخدم والنتيجة
   - عرض الوقت الدقيق

---

## 🔐 الأمان

✅ **TLS/SSL Encryption**
- جميع الاتصالات مشفرة
- حماية من التنصت

✅ **مصادقة المستخدمين**
- كلمات مرور مشفرة (bcrypt)
- نظام جلسات آمن

✅ **التحكم بالصلاحيات**
- أدوار مختلفة
- صلاحيات محدودة للمستخدمين العاديين

---

## 🔌 تقنيات مستخدمة

- **Go** - لغة البرمجة
- **TLS/SSL** - التشفير
- **bcrypt** - تشفير كلمات المرور
- **HTML/CSS** - واجهة الويب
- **JSON** - تخزين المستخدمين

---

## 📚 أمثلة استخدام

### إنشاء موقع ويب افتراضي:
```bash
# تسجيل الموقع في DNS
go run dns_client.go REGISTER mywebsite.nexa 127.0.0.1 1413 web

# تخزين محتوى الموقع
go run client/client.go PUBLISH mywebsite.nexa "<!DOCTYPE html>..."

# جلب المحتوى من أي جهاز
go run client/client.go FETCH mywebsite.nexa
```

### نظام اتصالات آمن:
- جميع البيانات مشفرة عبر TLS
- كل طلب يحتاج مصادقة
- سجل كامل للعمليات

---

## 🐛 استكشاف الأخطاء

### مشكلة: "Connection refused"
- تأكد أن جميع الخوادم تعمل
- تأكد من أرقام المنافذ (1112, 1413, 8080)

### مشكلة: "DNS resolution failed"
- تأكد أن خادم DNS يعمل
- تحقق من اسم السجل المسجل

### مشكلة: "Invalid credentials"
- استخدم البيانات الصحيحة (admin / admin123)
- حرف صغير/كبير يهم

---

## 🚀 الخطوات التالية

1. **إضافة قاعدة بيانات حقيقية** (SQLite/MongoDB)
2. **دعم نقل الملفات** (Upload/Download)
3. **واجهة رسومية متقدمة** (Desktop App)
4. **API REST** بدل الواجهة النصية
5. **نشر على السحابة** (Docker, Kubernetes)
6. **تطبيق موبايل** (لـ iOS و Android)

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه وتعديله بحرية.

---

## 💬 التواصل والدعم

للأسئلة أو المشاكل، تأكد من:
- فحص السجلات (Logs)
- تشغيل جميع الخوادم
- استخدام البيانات الصحيحة
- تفعيل TLS في جميع الاتصالات

---

**تم تطوير Nexa بعناية فائقة لإعطاءك نظام شبكة احترافي وآمن وقابل للتطوير.**

🎉 استمتع بالاستخدام!
